from __future__ import annotations


def get_classify_prompt(default_prompt: str) -> str:
    # 这里留给你写：意图识别 prompt。
    # 当前先返回 node 里的旧 prompt，保证链路能跑。
    # 等你准备好后，可以在这里直接 return 你维护的新 prompt。
    return default_prompt

import asyncio
from langchain_core.messages import AIMessage
from app.api.chat import chat_stream, ChatRequest

async def main():
    # just testing the logic inside event_generator if possible
    # well, it's bound to the real agent graph.
    pass

if __name__ == "__main__":
    asyncio.run(main())
